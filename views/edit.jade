h1 Editing post: #{post.subject}

form(name="edit-post",method="post",action="/edit/post")
  
  input(type="hidden",name="id",value="#{post._id}")
  
  fieldset.details
    
    label(for="subject") Subject:
    input(type="text",name="subject",value="#{post.subject}", required)
    
    label(for="subtitle") Subtitle:
    input(type="text",name="subtitle", required, value="#{post.subtitle}")
    
    label(for="tags") Tags (comma-separated)
    input(type="text",name="tags",value="#{post.tags}")
  
  fieldset.textcontent
    
    label(for="body") Content
    textarea.postarea(name="body", resize="both") #{post.body}
    
    label(for="bodyrefs") Markdown link references
    textarea.postrefs(name="bodyrefs", resize="both") #{post.bodyrefs}
    
    input(type="submit", class="button", name="post", value="Save")
