h1.main NodeBlog - A blog in node.js

h3 NodeBlog is built using node.js, express, jade, and mongodb with mongojs

for post in blogPosts
  div.items
    a(href="/posts/#{post._id}")
      h2 #{post.subject}
    - var length = 200;
    - var trimmedBody = post.body.substring(0, length) + '...'; 
    p=  trimmedBody
    p.tags
      for tag in post.tags
        a.tag(href="/posts/tags/#{tag}") #{tag}
    p
      span.posted Posted: #{moment(post.created).format('DD.MM.YYYY HH:mm')}